server:
  port: 8080
spring:
  application:
    name: forum-discovery
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:${server.port}/forum-discovery/